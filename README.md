# Contentful test with nuxt

Testing contentful API with nuxt

to connect to your contentful environment create a `.env` file inside the base directory with the following content:

```
CONTENTFUL_SPACE_ID=<your space id>
CONTENTFUL_ACCESS_TOKEN=<your access token>
```
